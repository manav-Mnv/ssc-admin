# Copy to .env.local for local dev (vercel dev), or set via `vercel env add` for production.
# The Supabase PAT is server-side only — never expose it in the browser.

SUPABASE_PAT=your_supabase_personal_access_token
SUPABASE_PROJECT_REF=ffizrzdifznulnzbgpjy
ADMIN_KEY=your_admin_passcode
